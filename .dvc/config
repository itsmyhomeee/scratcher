[core]
    remote = minio
[cache]
    type = hardlink
['remote "minio"']
    url = s3://cian-photos/dvc-store
    endpointurl = http://localhost:9001
    access_key_id = admin
    secret_access_key = password123
